namespace Course
{
    // Define uma interface chamada IResizable
    interface IResizable
    {
        // Declaração de um método chamado Resize que aceita um parâmetro do tipo double
        // Este método deve ser implementado por qualquer classe que implemente esta interface
        void Resize(double scale);
    }
}
